---
name: orchestrate
description: Orchestrate subagents to make requested changes correctly, using as many as needed. Use only when explicitly invoked.
disable-model-invocation: true
---

Can you orchestrate subagents to make those changes? Use as many as you need and do it correctly
